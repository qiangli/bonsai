#
priorart
reference

# Build artifacts
/build
/installs
/coverage.out
/bonsai
/dgraph

# Stale per-subcommand binaries from the pre-merger layout (cmd/bonsai-{server,cli,bulk,live}).
# Kept here so an accidental `go build ./cmd/...` from old shell history doesn't get committed.
/bonsai-server
/bonsai-cli
/bonsai-bulk
/bonsai-live

# IDE
.idea
.vscode
*.iml

# Binaries
*.exe
*.exe~
*.dll
*.so
*.dylib

# Test
*.test
*.out

# OS
.DS_Store

# Misc
vendor
.minio.sys
# Stale binaries from the dgraph2 → bonsai rename. Listed so an accidental
# `go build ./cmd/dgraph2-server` (e.g. from old shell history) doesn't get
# committed.
/dgraph2-server
/dgraph2-cli
/dgraph2-bulk
/dgraph2-live
/.gfy-out/
